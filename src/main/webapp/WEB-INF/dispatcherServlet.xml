<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- MVC 패턴 사용하고 싶으면... 즉, @Controller 애노테이션을 사용하고 싶으면 적어주셔야 함 -->
    <mvc:annotation-driven />

    <!-- 애노테이션을 사용하고 싶으면 -->
    <context:annotation-config />

    <!-- 애노테이션으로 빈 등록할 클래스들을 스캔뜨는 시작 패키지를 설정 -->
    <context:component-scan base-package="com.koreait.alsamo" />

    <mvc:resources mapping="/res/**" location="/res/" />
    <mvc:resources mapping="/img/**" location="file:///D:/springImg/" />

    <!--인터셉터 추가될때마다 <mvc:interceptor>로 추가 -->
    <mvc:interceptors>
        <mvc:interceptor>
            <!--/이하 경로들 모두 인터셉터 통과-->
            <mvc:mapping path="/board/writeMod"/>
            <mvc:mapping path="/board/favList"/>
            <mvc:mapping path="/user/profile"/>

            <bean class="com.koreait.alsamo.user.AuthCheckIntercepter"/><!--인터셉터 셋팅 파일 잡아주는-->
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 사용할 파일명을 완성해주는 객체 -->
    <bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/views/" />
        <property name="suffix" value=".jsp" />
    </bean>



</beans>